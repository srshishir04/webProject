<!DOCTYPE html>
<html>
<head>
    <title>Flight Game</title>
    <style>
        .dropdown {
            margin-bottom: 15px;
            font-size: 20px;
        }
        #map {
            margin-top: 30px;
        }
        .submit-button {
            margin-left: 90px;
        }
    </style>
    <script>
      var mapCenter = [{{ map_center[0] }}, {{ map_center[1] }}];
      function initMap() {
        if (mapCenter) {
          var map = new google.maps.Map(document.getElementById('map'), {
            zoom: 8,
            center: {lat: mapCenter[0], lng: mapCenter[1]},
          });
          var marker = new google.maps.Marker({
            position: {lat: mapCenter[0], lng: mapCenter[1]},
            map: map,
          });
        }
      }
    </script>
    <script async defer
            src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBSLdg6JGi64DtPk-0E91muK89wsXDuCrY&callback=initMap"></script>
</head>
<body onload="initMap()">
<h1>Flight Database Search</h1>
<form method="POST">
    <div class="dropdown">
        <label for="country"><b>Country Search</b></label></br>
        <select name="country" id="country">
            {% for country in countries %}
            <option value="{{ country.iso_country }}" {% if selected_country== country.iso_country %} selected {% endif %}>
                {{ country.name }}
            </option>
            {% endfor %}
        </select>
    </div>
    <div class="dropdown">
        <label for="airport"><b>Airport Search</b></label></br>
        <select name="airport" id="airport">
            {% for airport in airports %}
            <option value="{{ airport.ident }}" {% if selected_airport== airport.ident %} selected {% endif %}>
                {{ airport.name }}
            </option>
            {% endfor %}
        </select>
    </div>
    <div class="submit-button">
        <input type="submit" value="Submit">
    </div>
</form>
<div id="map" style="height: 400px; width: 100%;"></div>
</body>
</html>
